import React from 'react'
import ReactDOM from 'react-dom'
import { render, fireEvent, cleanup } from '@testing-library/dom'
import Radios from './Radios'
import App from './../../../App'

afterEach(cleanup)

// it('Should render radios buttons.', () => {
//   const {  }
// })
